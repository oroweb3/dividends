# Sponsored principal test — September 2026

One explicitly authorized small NVDAx-to-GOLD principal swap finalized successfully. This was not a dividend conversion. The user's SOL balance was unchanged; the sponsor covered the network fee and GOLD token-account creation.

The test exercised Titan instruction validation, repeated unsigned simulation over pinned bytes, Privy ownership/delegation and sponsor verification, exact-message signature verification, durable journaling before broadcast, and finalized transaction-effect verification.

The authorization is consumed and the one-off script is retired. Wallet associations, exact holdings, transaction identifiers and signed bytes remain in private operator records. No public checkout is authorized to repeat this test. The live test does not establish dividend eligibility; the separate automated lifecycle suite uses controlled provider responses and isolated database fixtures.
